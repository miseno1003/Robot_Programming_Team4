"""네비게이션 좌표 및 접근 동작 파라미터.

현장에서 맵을 확인한 뒤 좌표값(특히 ROOM_GOALS, GUARD_ROOM_GOAL, HOME_POSITION)을
실제 값으로 채워야 한다.
"""

import math
import numpy as np

# =====================================================================
# 초기(홈) 위치 — 모든 미션 완료 후 복귀할 위치
# =====================================================================
HOME_POSITION = {
    'x': 0.045358200634559295,
    'y': -0.8891374562791822,
    'yaw_deg': 0,
}

# =====================================================================
# 경비실 위치 — VLM 이 '경비실'로 인식하면 이 좌표로 이동
# =====================================================================
GUARD_ROOM_GOAL = {
    'x': -0.788575285964446,
    'y': -0.9319413029129134,
    'yaw_deg': 0,
}

# =====================================================================
# 호수별 목표 좌표 — key 는 VLM 이 읽어낼 호수 숫자와 일치
# =====================================================================
ROOM_GOALS = {
    101: {'x': None, 'y': None, 'yaw_deg': None},   # TODO: 101호 좌표 입력
    102: {'x': -1.5886486448997363, 'y': -0.04895991678570115, 'yaw_deg': 0},
    103: {'x': -1.5886486448997363, 'y': -0.04895991678570115, 'yaw_deg': 90},
    104: {'x': -1.7061288140109718, 'y': -1.6919609490537602, 'yaw_deg': 180},
    105: {'x': -1.7061288140109718, 'y': -1.6919609490537602, 'yaw_deg': -90},
    106: {'x': -0.37581060534756844, 'y': -1.6399270251913531, 'yaw_deg': 0},
}

# 목표 프레임 (보통 'map')
GOAL_FRAME_ID = 'map'


# =====================================================================
# 복귀 관련
# =====================================================================
BELL_PRESSED_STOP_SEC = 3.0       # 벨 누른 후 정지 시간 [초]
REVERSE_DISTANCE_M = 0.15         # 후진 거리 [m]
REVERSE_SPEED = 0.05              # 후진 속도 [m/s] (양수, 내부에서 음수 적용)
NO_BELL_STOP_SEC = 2.0            # 벨 미누름 시 180도 회전 후 정지 시간 [초]
HALF_ROTATE_ANGULAR = 0.5        # 제자리 180도 회전 속도 [rad/s]


# =====================================================================
# 빨간색 검출 (HSV)
# =====================================================================
RED_LOWER1 = np.array([0, 100, 80])
RED_UPPER1 = np.array([10, 255, 255])
RED_LOWER2 = np.array([160, 100, 80])
RED_UPPER2 = np.array([179, 255, 255])

MIN_RED_AREA = 150
POSITION_CENTER_BAND = 0.10


# =====================================================================
# 정렬/회전 게인
# =====================================================================
ROTATE_GAIN = 0.5
ROTATE_MAX = 0.4
ROTATE_MIN = 0.08
ROTATE_MAX_FINE = 0.20
ROTATE_MIN_FINE = 0.05


# =====================================================================
# 접근 단계 설정
# =====================================================================
APPROACH_STAGES = [
    {'target': 0.50, 'name': '50cm 정렬',     'tol': 0.10, 'confirm': 1, 'fine': False},
    {'target': 0.35, 'name': '35cm 접근',     'tol': 0.08, 'confirm': 1, 'fine': False},
    {'target': 0.25, 'name': '25cm 정밀정렬', 'tol': 0.03, 'confirm': 3, 'fine': True},
]

STOP_TOLERANCE = 0.02

FINAL_PUSH_DISTANCE = 0.08
FINAL_PUSH_SPEED = 0.02
FINAL_PUSH_TIME = FINAL_PUSH_DISTANCE / FINAL_PUSH_SPEED

LIDAR_FRONT_ANGLE = 0
LIDAR_FRONT_WINDOW_DEG = 15.0

ALIGN_STEP_TIME = 0.15
ALIGN_STEP_TIME_FINE = 0.10

SEARCH_ANGULAR = 0.35
SEARCH_STEP_TIME = 0.4
SEARCH_FULL_TURN = 2.0 * math.pi

MAX_VERIFY_ATTEMPTS = 60
